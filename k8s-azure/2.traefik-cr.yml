kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1 # ändrade till denna, artikelns var deprecated
metadata:
  name: traefik-ingress
rules:
  - apiGroups:
      - ""
    resources:
      - services
      - endpoints
      - secrets
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - extensions
    resources:
      - ingresses
    verbs:
      - get
      - list
      - watch